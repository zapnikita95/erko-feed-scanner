# Railway / production
PORT=4173
NODE_ENV=production
SESSION_SECRET=change-me-to-random-32-plus-chars
DATA_DIR=/data

# Ограничение доменов email при входе (через запятую)
ALLOWED_EMAIL_SUFFIXES=@diginetica.com,@anyquery.ru

# Опционально
FETCH_TIMEOUT_MS=120000
